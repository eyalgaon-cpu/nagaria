name: Apply Update
on:
  push:
    branches: [main]
    paths: ['commands/pending.json']

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run update
        run: python3 apply.py
